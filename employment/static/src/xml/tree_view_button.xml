<?xml version="1.0" encoding="UTF-8"?>
    <template id="my_contacts_template" xml:space="preserve">
          <t t-extend="KanbanView.buttons">
            <t t-jquery="button" t-operation="after">
                <button t-if="widget.modelName == 'requirement.educate'" class="btn btn-primary my-contacts-button" type="button">My Contacts</button>
            </t>
          </t>
    </template>
<button name="action_confirm" id="action_confirm" string="Confirm" class="btn-primary" type="object" attrs="{'invisible': [('state', 'not in', ['sent'])]}"/>
<button name="action_confirm" string="Confirm" type="object" attrs="{'invisible': [('state', 'not in', ['draft'])]}"/>
<button name="action_cancel" type="object" string="Cancel" attrs="{'invisible': ['|', ('state', 'not in', ['draft', 'sent','sale']), ('id', '=', False)]}"/>
<button name="action_draft" states="cancel" type="object" string="Set to Quotation"/>


<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="seq_code_medicalrecord" model="ir.sequence">
            <field name="name">Code Medical Record Sequence</field>
            <field name="code">medical.record</field>
            <field name="prefix">HSBA</field>
            <field name="padding">4</field>
            <field name="company_id" eval="False"/>
        </record>
    </data>
</odoo>

<div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>